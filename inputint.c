#include <stdio.h>

	int main()
	{
		int number, x, y;
		
		printf("Type in a number \n");
		scanf("%d", &number \ n");
		printf("The number you typed was %d\n", number);
		x = nuber * number ;
		printf("number * number is %d\n",x);
		return 0;
	}
